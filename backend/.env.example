# Backend Environment Variables
PORT=8000
HOST=0.0.0.0
CORS_ORIGINS=*

# Optional Supabase credentials (not required if using local SQLite database)
# SUPABASE_URL=https://duoesappjstgejlwxkyp.supabase.co
# SUPABASE_KEY=sua-chave-anon

# Optional Battlemat Bridge Host (if running Digital Battlemat separately)
# BATTLEMAT_HOST=localhost:8080
